import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
   content: {
     flex: 4,
     alignItems: 'center',
     paddingTop: '5%',
     textAlign: 'center',
   },
 });

export default Styles