import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
   header: {
      flex: 1.9,
      backgroundColor: '#fa1100',
      alignItems: 'center',
      justifyContent: 'center',
    },
    headerText: {
      color: 'white',
      fontSize: '1.1rem',
      fontWeight: '500',
    },
})

export default styles